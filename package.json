{
  "name": "apartment-compare",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first apartment comparison app with SQLite storage.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:live": "LIVE_ZILLOW_TEST=1 node --test --test-name-pattern='imports Zillow listing data from the live listing URL'"
  },
  "dependencies": {
    "better-sqlite3": "^12.1.1",
    "cheerio": "^1.1.2",
    "express": "^5.1.0",
    "tesseract.js": "^6.0.1"
  },
  "devDependencies": {
    "supertest": "^7.1.1"
  }
}
